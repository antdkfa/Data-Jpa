spring:
  datasource:
    url: jdbc:h2:file:~/tododb
    driver-class-name: org.h2.Driver
    username: sa
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

logging.level:
  org.hibernate.SQL: debug